<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TickTick" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/main_accent_blue</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:windowBackground">@color/main_bg</item>
        <item name="android:statusBarColor">@color/main_bg</item>
        <item name="android:navigationBarColor">@color/main_bottom_nav_bg</item>
    </style>

    <style name="Theme.TickTick" parent="Base.Theme.TickTick" />

    <!-- Splash Screen Theme -->
    <style name="Theme.TickTick.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_gradient_bg</item>
        <item name="android:statusBarColor">@color/splash_bg_dark</item>
        <item name="android:navigationBarColor">@color/splash_bg_dark</item>
    </style>

    <style name="BottomSheetDialogTheme" parent="Theme.Material3.DayNight.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowSoftInputMode">adjustResize</item>
    </style>

    <style name="BottomSheetStyle" parent="Widget.Material3.BottomSheet.Modal">
        <item name="backgroundTint">@color/main_surface</item>
        <item name="shapeAppearanceOverlay">@style/BottomSheetRounded</item>
    </style>

    <style name="BottomSheetRounded" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopLeft">16dp</item>
        <item name="cornerSizeTopRight">16dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
    </style>

    <style name="CircleImage" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item> <!-- 50% sẽ tạo ra hình tròn hoàn hảo -->
    </style>

    <style name="TimePickerDarkTheme" parent="ThemeOverlay.Material3.MaterialTimePicker">
        <!-- Dialog background -->
        <item name="colorSurface">#2D2D2D</item>
        <item name="colorSurfaceContainerHigh">#2D2D2D</item>
        <!-- Text trên dialog -->
        <item name="colorOnSurface">#E0E0E0</item>
        <item name="colorOnSurfaceVariant">#B0B0B0</item>
        <!-- Accent (selected state) -->
        <item name="colorPrimary">#4C6FE0</item>
        <item name="colorOnPrimary">#FFFFFF</item>
        <!-- Input fields -->
        <item name="colorPrimaryContainer">#3A3A3A</item>
        <item name="colorOnPrimaryContainer">#FFFFFF</item>
        <!-- Tertiary (unselected input) -->
        <item name="colorTertiaryContainer">#3A3A3A</item>
        <item name="colorOnTertiaryContainer">#B0B0B0</item>
        <!-- Clock face -->
        <item name="colorSurfaceVariant">#3A3A3A</item>
    </style>

    <style name="Theme.TickTick.FullScreenDialog" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@color/main_surface</item>
    <!-- Tránh top status bar cover mất content -->
</style>
</resources>